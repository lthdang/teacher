version: "3.9"

services:
  teacher-db:
    image: postgres:17
    container_name: ${COMPOSE_PROJECT_NAME}.teacher-db
    restart: always
    volumes:
      - ./data/postgres:/var/lib/postgresql/data
      - ./data/backup:/home/backup
    ports:
      - "${POSTGRES_ACCESS_PORT}:5432"
    env_file:
      - .env
